{#
 # Homepage
 # =============================================================================
 #}

{% if entry is not defined %}
	{% exit 404 %}
{% endif %}

{% extends "_layouts/standard" %}

{% block content %}
	<main id="content">
		<h1>{{ entry.title }}</h1>
	</main>
{% endblock %}
